using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MovementOrizontalBar : MonoBehaviour
{

    void Start()
    {
        transform.position = new Vector3(3, 0.85f, 10.75f);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
    
}
